[pytest]
testpaths =
    tests/unit
    tests/integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: 端到端集成测试（可能依赖真实模型 / 文件 IO）
    parity: 行为一致性测试（重构前后语义对齐）
addopts =
    -v
    --strict-markers
    --tb=short
