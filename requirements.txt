# FunASR related
funasr==1.2.7
modelscope==1.18.0
torch==2.8.0
torchaudio==2.8.0
onnxruntime>=1.14.0
numpy==2.3.3

# Web service related
websockets==12.0
aiofiles==23.2.1
uvloop==0.19.0; sys_platform != 'win32'

# Database
aiosqlite==0.19.0

# Configuration and logging
pydantic==2.5.3
loguru==0.7.2
python-multipart==0.0.6
python-dotenv==1.0.0

# Audio/Video processing
ffmpeg-python==0.2.0
soundfile==0.12.1
# librosa: 仅在 Qwen3-Diarize 引擎对非 16kHz 音频做重采样时触发(diarize 入口)
librosa==0.11.0

# Qwen3-Diarize 引擎依赖 (vendor: src/core/vendor/qwen_asr_gguf)
# - sherpa-onnx: speaker diarization (pyannote 分段 + NeMo TitaNet embedding)
# - gguf: qwen_asr_gguf 引擎读 GGUF LLM 权重元数据
# - srt: qwen_asr_gguf 引擎内部的字幕格式工具
sherpa-onnx==1.13.2
gguf==0.19.0
srt==3.5.3

# 词级时间戳 (word_align, MMS-300M CTC-FA)
# - ctc-forced-aligner: deskpai ONNX fork, 多语种 CTC 强制对齐 (走 onnxruntime)
# - unidecode: ctc-forced-aligner 罗马化 (get_uroman_tokens) 依赖
# 模型 ONNX (~1.2GB) 由 scripts/download_qwen3_models.sh 预下, 不进 pip
#
# ⚠️ CUDA 部署陷阱: ctc-forced-aligner 依赖声明的是 CPU 版 `onnxruntime`, pip install
#    会把它装上并**覆盖** `onnxruntime-gpu` 的 onnxruntime/capi/*.so → CUDAExecutionProvider
#    消失 (ORT 退回 CPU). CUDA 机器装完本 requirements 后必须修复:
#       pip uninstall -y onnxruntime
#       pip install --force-reinstall --no-deps onnxruntime-gpu==<原版本>
#    (onnxruntime-gpu 提供同名 `import onnxruntime`, API 兼容, ctc 仍能跑.)
#    Mac/CPU 部署无此问题 (本就用 CPU onnxruntime). 见 docs 部署 + 3060 probe 记录.
ctc-forced-aligner==1.0.2
unidecode==1.4.0

# HTTP client (WeChat Work notification)
httpx==0.26.0

# Utilities
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=3.2.0,<4.0.0
aioredis==2.0.1

# Development tools
pytest==7.4.4
pytest-asyncio==0.23.3
black==23.12.1
isort==5.13.2

psutil