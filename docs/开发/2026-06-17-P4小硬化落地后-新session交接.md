# 新 session 交接:P4 小硬化 + #24 分片 e2e 落地后的后续工作

> 2026-06-17(第二轮)。本轮完成 A(P4 小硬化 + P3 质量闸)+ C(#24 分片 e2e 安全网)
> 全闭环并 push origin/main。本文交接剩余 backlog,新 session 读完即可接手。

## 一、本轮做完了什么(已 push main = `f90c8d1`,勿重做)

- **C / TODOS #24 分片上传 e2e 盲区**:
  - `tests/unit/test_websocket_chunk_upload.py` — `_handle_chunk_upload` 7 条逐帧路径(missing/session_not_found/duplicate/hash_mismatch/落盘计数/收齐触发 finalize)。
  - `tests/integration/test_chunked_upload_e2e.py` + `_ws_e2e_common.client_chunked_upload_and_wait` — 真帧序列穿 server→分片重组→finalize→`_process_task`,多分片全流程 + 缓存命中。
- **P4 A1 DB 缓存 size-cap**:`DatabaseConfig.max_cache_count`(默认 5000,env `FUNASR_MAX_CACHE_COUNT`,`<=0` 关)+ `idx_accessed_at` 索引 + `clean_old_cache` 两阶段(TTL→LRU)。**顺修既有 regression**:时间戳格式 bug(`isoformat()` 的 `T` vs DB 空格,误删临界同日行),改 `strftime` 对齐 + 回归测试。
- **P4 F1**:抽 `_maybe_delete_task_file(task, reason=)` 去重 cancel/complete/failed 三处删文件逻辑;`file_utils.delete_file` 返 `bool`。
- **P4 A2 孤儿文件 sweeper**:`task_manager._sweep_orphan_upload_files()` 接 `_maintenance_loop`。**codex 二审推翻原 evict 兜底方案**(撞 worker race / 重启盲区 / 无重试)→ 改文件系统 sweeper(扛重启 + 删失败重试,宽限期 `orphan_file_grace_seconds` 默认 7200=2h)。
- **P4 A3 pm2 startup**:核查发现 `com.PM2` launchd agent 配过但**未加载** → `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/pm2.zhanglixing.plist` 已注册,重启自起现在可靠。
- **P4 A4 / P3 质量闸**:`scripts/git-hooks/pre-push` + `scripts/install-git-hooks.sh`(已激活 `core.hooksPath=scripts/git-hooks`)。push 前跑 unit,碰 FunASR 核心路径自动追加 parity。紧急放行 `FUNASR_SKIP_PREPUSH=1 git push`。
- 设计/审查权威:`docs/开发/2026-06-17-P4小硬化-A1缓存sizecap-A2孤儿文件-落地计划.md`(含 eng-review + codex 二审报告)。
- **状态**:main = `f90c8d1`,github 已同步。839 unit + 6 parity 全绿。prod 运行中(可观测性 P1 已部署,本轮 A1/A2 代码**尚未部署到 prod**,见下方"部署待办")。

## 二、剩余 backlog(按推荐优先级)

### 推荐先做(配新指标 / 碰 task_manager,主题连贯)
- **TODOS #22 in-flight file_hash 去重**(P2):双客户端撞同一文件,pool=1 白跑一个槽。依赖已满足。
- **TODOS #3 异常分类纪律**(P2):现在全包成普通 Exception + 字符串匹配判 retryable。配合可观测性的 `errors_total{kind}` 指标,分类后仪表盘错误维度才有意义。**建议与 #22 一起做**(都碰 task_manager,一次 plan-eng-review + 一次 parity)。

### 技术债(非紧急,codex 长期提)
- #5 websocket_handler 拆分(~670 行,纯重构,需专注独立 session)。
- #7 自建 ~585 行 IPC 池换成熟库(核心 IPC,高风险)。
- #12 Pydantic 弃用 API 清理(`.dict()`→`.model_dump()`,日志一直 warn;`websocket_handler.py:387/418` 等)。

### 可观测性自然延伸
- 告警:脚本定时拉 `/metrics`,错误率/队列深度超阈值发企微(已有 webhook 基建)。
- TODOS #23 冷启动 /health 可观测(启动卡死时 /health 也能答,需动 main.py 启动序)。
- 孤儿 sweeper 回收数 / 缓存 size-cap 淘汰数进 `/metrics`(目前只进日志)。

## 三、部署待办(重要)

本轮 A1(database.py)/A2(task_manager.py)代码**只在 dev,未上 prod**。何时部署由你定;若部署:
```bash
cd ~/Production/funasr_spk_server && git pull origin main && pm2 restart funasr-server
```
新 config 默认值(max_cache_count=5000 / orphan_file_grace_seconds=7200)无需改 .env 即生效。

## 四、关键上下文 / 环境坑(务必先读)

- **严格 TDD**:先红 → 绿 → commit,最小单位提交,不积累多改动一次提交。
- **不开 PR**:本项目独立维护,完成后直接 push / merge main(单人 main 工作流)。
- **改 FunASR 路径必跑 parity**(schemas/database/task_manager/websocket_handler/funasr_transcriber):
  `bash scripts/run_checks.sh --parity`。**pre-push hook 现在会自动跑**(碰这些路径时),但本地随手验也用它。
- **pytest 用 `venv/bin/python -m pytest`**(venv pytest binary shebang 漂移)。
- **dev 机 .env 污染**:纯净环境 6 个 config 测试会因 dev .env 误挂;unit 有 hermetic fixture 兜底,parity 要显式 `FUNASR_DEFAULT_ENGINE=funasr`(run_checks.sh 已带);ort_parity 在 Mac 上必挂(正常)。
- **PM2 / launchctl 命令走 Bash 工具**(不要走 ctx_execute,会污染 TMPDIR 导致 daemon ENOENT)。
- **部署**(macOS only,不走 docker):prod `~/Production/funasr_spk_server`(8767,PM2),dev 本目录(8867,前台)。
- **架构权威**:项目根 `CLAUDE.md`(引擎/config/队列/孤儿 sweeper/缓存清理/可观测性/pre-push 闸各章节)。

## 五、建议的新 session 开场

最连贯的切入:**#22 in-flight 去重 + #3 异常分类**(配上一轮 `errors_total` 指标,都碰 task_manager,一个 session 一起做)。碰核心路径,先 `/plan-eng-review`。技术债 #5/#7 各自独立 session。
